<%= form_with(model: waiter, local: true) do |form| %>
  <% if waiter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(waiter.errors.count, "error") %> prohibited this waiter from being saved:</h2>

      <ul>
      <% waiter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :restaurant_id %>
    <%= form.text_field :restaurant_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
